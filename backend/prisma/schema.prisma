generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model JournalEntry {
  id Int @id @default(autoincrement())
  mood String
  aiResponse String
  createdAt DateTime @default(now())
}
